import { PageTitle, StyledWrapper } from "../../globalStyles";
const Projects = () => {
  return (
    <section>
      <StyledWrapper>
        <PageTitle>Projects</PageTitle>
      </StyledWrapper>
    </section>
  );
};

export default Projects;
